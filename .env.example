# Main project's docker postgres (pgvector/pgvector:pg16)
# Run `docker compose up postgres` in iterate-to-wealth/ first.
MAIN_DATABASE_URL=postgresql+psycopg://invest_engine:invest_engine@localhost:5432/invest_engine

# OpenAI for gpt-4o-mini
OPENAI_API_KEY=sk-...
